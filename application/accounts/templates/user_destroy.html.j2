{% extends "base.html.j2" %}

{%- block workspace -%}
  {%- from 'macros/destroy.html.j2' import render_destroy_dialog, render_destroy_page with context -%}

  {%- if request.query_params.dialog -%}
    {{ render_destroy_dialog(form, "users", "用户") }}
  {%- else -%}
    {{ render_destroy_page(form, "users", "用户") }}
  {%- endif -%}
{%- endblock -%}
